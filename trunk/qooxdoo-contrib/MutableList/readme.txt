MutableList
=========================

A list along with a text field and some basic operations that allow the
user to input new entries, edit and delete old entries, and that allows
the programmer to retrieve the list of entries.
